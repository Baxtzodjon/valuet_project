import React from "react";
import { Link } from "react-router-dom";

function Home() {

    return (
        <>

            {/* <h1 className="bg-red-500">Home</h1>

            <Link to={"/sign-in"}>Login</Link>

            <Link to={"/sign-up"}>Log Out</Link>

            <ul>

                {user.map((user) => <li key={user.id}>{user.name} {user.surname}</li>)}

            </ul> */}

        </>
    )
}

export default Home;